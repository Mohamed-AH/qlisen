# Whisper Model Selection
# Options: tiny, base, small, medium, large
# Optimized for Quranic recitation: small (best accuracy for Classical Arabic)
# Performance: 10s audio → 5-6s processing (acceptable for accuracy priority)
WHISPER_MODEL=small

# Port Configuration (don't change unless port 5000 is already in use)
WHISPER_PORT=5000
